{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "differentiated_input_names_substitute": {
      "type": "array",
      "description": "The names of the discipline inputs with respect to which to differentiate the discipline outputs used as objective, constraints and observables. If empty, consider the inputs of these functions. More precisely, for each function, an [ArrayFunction][gemseo.core.functions.array_function.ArrayFunction] is built from the `disciplines`, which depend on input variables $x_1,\\ldots,x_d,x_{d+1}$, and over an input space spanned by the input variables $x_1,\\ldots,x_d$ and depending on both the MDO formulation and the `design_space`. Then, the methods [ArrayFunction.evaluate()][gemseo.core.functions.array_function.ArrayFunction.evaluate] and [ArrayFunction.jac()][gemseo.core.functions.array_function.ArrayFunction.jac] are called at a given point of the input space and return the output value and the Jacobian matrix, i.e. the matrix concatenating the partial derivatives with respect to the inputs $x_1,\\ldots,x_d$ at this point of the input space. This argument can be used to compute the matrix concatenating the partial derivatives at the same point of the input space but with respect to custom inputs, e.g. $x_{d-1}$ and $x_{d+1}$. Mathematically speaking, this matrix returned by [ArrayFunction.jac()][gemseo.core.functions.array_function.ArrayFunction.jac] is no longer a Jacobian."
    },
    "main_mda_settings": {
      "type": "object",
      "properties": {
        "coupling_structure": {
          "type": "null"
        },
        "linear_solver_settings": {
          "type": "object",
          "properties": {
            "store_residuals": {
              "type": "boolean"
            },
            "use_ilu_precond": {
              "type": "boolean"
            },
            "save_when_fail": {
              "type": "boolean"
            }
          },
          "required": [
            "save_when_fail",
            "store_residuals",
            "use_ilu_precond"
          ]
        },
        "log_convergence": {
          "type": "boolean"
        },
        "max_mda_iter": {
          "type": "integer"
        },
        "max_consecutive_unsuccessful_iterations": {
          "type": "integer"
        },
        "name": {
          "type": "string"
        },
        "tolerance": {
          "type": "number"
        },
        "use_lu_fact": {
          "type": "boolean"
        },
        "warm_start": {
          "type": "boolean"
        },
        "acceleration_method": {
          "type": "string"
        },
        "over_relaxation_factor": {
          "type": "number"
        },
        "execute_before_linearizing": {
          "type": "boolean"
        },
        "n_processes": {
          "type": "integer"
        },
        "use_threading": {
          "type": "boolean"
        },
        "chain_linearize": {
          "type": "boolean"
        },
        "inner_mda_settings": {
          "type": "object",
          "properties": {
            "coupling_structure": {
              "type": "null"
            },
            "linear_solver_settings": {
              "type": "object",
              "properties": {
                "store_residuals": {
                  "type": "boolean"
                },
                "use_ilu_precond": {
                  "type": "boolean"
                },
                "save_when_fail": {
                  "type": "boolean"
                }
              },
              "required": [
                "save_when_fail",
                "store_residuals",
                "use_ilu_precond"
              ]
            },
            "log_convergence": {
              "type": "boolean"
            },
            "max_mda_iter": {
              "type": "integer"
            },
            "max_consecutive_unsuccessful_iterations": {
              "type": "integer"
            },
            "name": {
              "type": "string"
            },
            "tolerance": {
              "type": "number"
            },
            "use_lu_fact": {
              "type": "boolean"
            },
            "warm_start": {
              "type": "boolean"
            }
          },
          "required": [
            "coupling_structure",
            "linear_solver_settings",
            "log_convergence",
            "max_consecutive_unsuccessful_iterations",
            "max_mda_iter",
            "name",
            "tolerance",
            "use_lu_fact",
            "warm_start"
          ]
        },
        "initialize_defaults": {
          "type": "boolean"
        },
        "mdachain_parallel_settings": {
          "type": "object"
        },
        "mdachain_parallelize_tasks": {
          "type": "boolean"
        },
        "sub_coupling_structures": {
          "type": "array"
        }
      },
      "required": [
        "acceleration_method",
        "chain_linearize",
        "coupling_structure",
        "execute_before_linearizing",
        "initialize_defaults",
        "inner_mda_settings",
        "linear_solver_settings",
        "log_convergence",
        "max_consecutive_unsuccessful_iterations",
        "max_mda_iter",
        "mdachain_parallel_settings",
        "mdachain_parallelize_tasks",
        "n_processes",
        "name",
        "over_relaxation_factor",
        "sub_coupling_structures",
        "tolerance",
        "use_lu_fact",
        "use_threading",
        "warm_start"
      ],
      "description": "The settings of the main MDA.  These settings may include those of the inner-MDA."
    },
    "parallel_scenarios": {
      "type": "boolean",
      "description": "Whether to run the sub-scenarios in parallel."
    },
    "multithread_scenarios": {
      "type": "boolean",
      "description": "If `True` and parallel_scenarios=True, the sub-scenarios are run in parallel using multi-threading; if False and parallel_scenarios=True, multiprocessing is used."
    },
    "apply_constraints_to_sub_scenarios": {
      "type": "boolean",
      "description": "Whether the constraints are added to the optimization problem of the sub-scenario capable of computing the constraint."
    },
    "apply_constraints_to_system": {
      "type": "boolean",
      "description": "Whether the constraints are added to the optimization problem of the system scenario."
    },
    "reset_x0_before_opt": {
      "type": "boolean",
      "description": "Whether to restart the sub-optimizations from the initial guesses, otherwise warm start them."
    },
    "set_x0_before_opt": {
      "type": "boolean",
      "description": "Whether to warm start the sub-optimizations from the disciplines' data, otherwise from the design space."
    },
    "sub_scenarios_log_level": {
      "type": "null",
      "description": "The level of the root logger during the sub-scenarios executions. If `None`, do not change the level of the root logger."
    },
    "keep_opt_history": {
      "type": "boolean",
      "description": "Whether to keep database copies of the sub-scenario adapters after each execution. Depending on the size of the databases and the number of consecutive executions, this can be very memory consuming. If the adapter will be executed in parallel, the databases will not be saved to the main process by the sub-processes, so this setting should be set to `False` to avoid unnecessary memory use in the sub-processes."
    },
    "save_opt_history": {
      "type": "boolean",
      "description": "Whether to save the optimization history to an HDF5 file after each execution."
    },
    "naming": {
      "type": "string",
      "description": "The way of naming the database files. When the adapter will be executed in parallel, this method shall be set to `UUID` because this method is multiprocess-safe."
    },
    "use_mda1": {
      "type": "boolean",
      "description": "Whether to use the first MDA. WARNING: this option may only lead to a converging formulation if you handle the couplings in the sub-optimizations (i.e. in a distributed  MDF fashion)."
    },
    "use_mda2": {
      "type": "boolean",
      "description": "Whether to use the second MDA."
    },
    "mda1_instance": {
      "type": "null",
      "description": "The first MDA. If `None` and if a first MDA is required, the formulation creates it. This MDA can also be an instance of `Discipline` that computes the strong couplings."
    },
    "mda2_instance": {
      "type": "null",
      "description": "The second MDA. If `None`, the formulation creates it. This MDA can also be an instance of `Discipline` that computes outputs (objective and constraints) for the system level optimizer."
    },
    "disciplines_as_sub_scenario": {
      "type": "array",
      "description": "The disciplines to be treated as sub-scenarios and not as disciplines. Typically used when a discipline wraps a sub-optimization process that is not using a GEMSEO scenario. If empty, consider only the sub-optimization processes built from `disciplines`."
    }
  }
}
