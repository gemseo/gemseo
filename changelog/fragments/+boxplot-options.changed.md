- `Boxplot_Settings.options` no longer accepts `orientation`, `vert` and `labels`.
  Use the setting `use_vertical_bars` to set the orientation.
  Use the matplotlib option `tick_labels` instead of `labels`.
