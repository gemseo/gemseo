The temp workbook copy created when unpickling an `XLSDiscipline` with `copy_xls_at_setstate=True` is now removed when the discipline is garbage collected or the interpreter exits, instead of accumulating under `tempfile.gettempdir()`.
