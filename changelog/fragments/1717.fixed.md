The iso-probabilistic mapping of a random space, namely `RandomSpace.transform_vect` and `RandomSpace.untransform_vect`, relies on the joint probability distribution of the random variables instead of the marginal ones, so that the dependence between random variables defined by a copula is taken into account.
