`ParameterSpace.normalize_vect` and `ParameterSpace.unnormalize_vect` now rely on the joint probability distribution of the uncertain variables instead of the marginal ones, so that the dependence between random variables defined by a copula is taken into account.
