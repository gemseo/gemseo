`ParameterSpace.evaluate_cdf` was removed because it did not implement the CDF and its inverse, but the Rosenblatt transformation for internal use. Use `RandomSpace.variables[name].distribution` to get the distribution of a random variable of a random space (and then, its methods `compute_cdf` and `compute_inverse_cdf`).
