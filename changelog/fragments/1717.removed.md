`ParameterSpace.evaluate_cdf` was removed because it did not implement the CDF and its inverse, but the Rosenblatt transformation for internal use.
Use `ParameterSpace.distributions[name]` to get a marginal distribution (and then, its methods `compute_cdf` and `compute_inverse_cdf`).
