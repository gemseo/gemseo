BREAKING CHANGES:

- The `apply_cstr_tosub_scenarios` field of `BiLevel_Settings` was renamed to `apply_constraints_to_sub_scenarios`.
- The `apply_cstr_to_system` field of `BiLevel_Settings` was renamed to `apply_constraints_to_system`.
