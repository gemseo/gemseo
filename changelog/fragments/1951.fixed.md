`enable_parallel_execution` and the fast mode now also apply
to `ParallelDisciplineChain` and `CallableParallelExecution`,
which no longer parallelize over all the disciplines
when the global configuration asks for a serial execution.
