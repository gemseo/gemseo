The test suite now covers the `XLSDiscipline` import-error branch when `xlwings` is unavailable; the previous `test_missing_xlwings` actually exercised the Excel-unavailable branch and has been renamed to `test_excel_unavailable`.
