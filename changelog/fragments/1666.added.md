`EvaluationScenario` allows for the evaluation of disciplinary outputs,
over a design space,
with a DOE algorithm.
The multi-disciplinary process is specified through a formulation (`BaseFormulation`).
