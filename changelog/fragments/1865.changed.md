Since the hash of an array whose data type is `int32` (resp. `uint32`) is now computed as for `int64` (resp. `uint64`) on all platforms, a cache persisted with a previous version whose input data include 32-bit integer arrays leads to cache misses and recomputations, not to corruption.
