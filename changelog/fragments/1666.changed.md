- `BaseFormulation` now takes an `EvaluationProblem` as input argument.
- BREAKING CHANGE: `MDOScenario.save_optimization_history` was renamed to `MDOScenario.to_hdf` and `MDOScenario.to_ggobi`.
- BREAKING CHANGE: `MDOScenario.set_optimization_history_backup` was renamed to `MDOScenario.set_backup_settings`.
- BREAKING CHANGE: `MDOScenario.clear_history_before_execute` was renamed to `MDOScenario.clear_database_before_execute`.
- BREAKING CHANGE: `MDOScenario.to_dataset` exports all the samples in the case of a DOE algorithm, including replicates.
- BREAKING CHANGE: The output names must be passed as positional arguments in `EvaluationScenario.add_observable`, `MDOScenario.add_observable` and `MDOScenario.add_constraint`, e.g. `scenario.add_constraint("foo", "bar", value=1)` for outputs `"foo"` and `"bar"`.
- BREAKING CHANGE: The arguments `objective_name` and `maximize_objective` of `MDOScenario` have been removed to comply with the signature of the parent class `EvaluationScenario`; use the new method `add_objective` instead.
- BREAKING CHANGE: The arguments `objective_name` and `maximize_objective` of `OptAsMDOScenario` have been removed to comply with the signature of the parent class `MDOScenario`; use the new method `add_objective` instead.
- BREAKING CHANGE: `gemseo.scenarios.mdo_scenario` was renamed to `gemseo.scenarios.mdo`.
- The default value of the `name` argument of `Concatenate` is `""`; if so, its `name` attribute concatenates the names of the concatenated functions.
