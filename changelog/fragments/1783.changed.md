BREAKING CHANGES:

- The `ScalarDistributionMixin` mixin has been replaced by a `BaseUnivariateDistribution` class deriving from `BaseDistribution`.
- The `gemseo.uncertainty.distributions.base_settings` package was renamed to `gemseo.uncertainty.distributions.base_univariate_settings`.
- The modules `base_distribution`, `base_distribution_settings` and `base_distribution_fitter` of `gemseo.uncertainty.distributions` were renamed to `base`, `base_settings` and `base_fitter` respectively.
