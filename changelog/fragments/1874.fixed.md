A progress bar no longer logs its last status when it is garbage collected without having been
closed, and `BaseDriverLibrary.execute` now closes the progress bar even when the algorithm raises
an exception.
