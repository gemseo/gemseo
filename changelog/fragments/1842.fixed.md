`BaseSensitivityAnalysis.plot` now accepts the `directory_path` and `file_name` arguments, and `CorrelationAnalysis.plot_radar` now accepts the `standardize`, `sort` and `sorting_output` arguments, consistently with the other plotting methods.
