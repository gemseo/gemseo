- The test suite now relies on [syrupy-matplotlib](https://pypi.org/project/syrupy-matplotlib/) for figure comparison; `gemseo.utils.testing.helpers.image_comparison` has been removed. Tests should use the `snapshot_matplotlib` fixture; baselines live under `tests/**/__snapshots__/<module>/` and are regenerated with `pytest --snapshot-update`.
