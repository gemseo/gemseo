`RemappingDiscipline` no longer requires a default value for every input variable of the wrapped
discipline; only the input variables mapped component-wise, e.g. `{"x": ("a", 0)}`, need one.
Values that are not NumPy arrays, e.g. the floats handled by an `AutoPyDiscipline`, are supported.
