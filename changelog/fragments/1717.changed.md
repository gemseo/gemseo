BREAKING CHANGE: The `build_joint_distribution` method of `ParameterSpace` has been removed and replaced by the `add_copula` method; the latter allows to add copulas associated with independent groups of dependent random variables.
