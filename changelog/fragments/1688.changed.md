An `EvaluationScenarioAdapter` (and hence an `MDOScenarioAdapter`) with `set_x0_before_exec=True` and `input_names` covering only a part of the design variables sets the starting point of the scenario from these inputs and the current value of the other design variables, projected into the bounds when `set_bounds_before_exec=True`, instead of raising an error.
