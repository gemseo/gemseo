- `FORMAnalysis` computes sensitivity indices from the importance factors
  of the first-order or second-order reliability method (FORM/SORM),
  interpreted as the sensitivity of events to the uncertain input variables.
- `EvaluationScenario.to_dataset` stores the execution result in `dataset.misc["execution_result"]`.
