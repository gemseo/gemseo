`DisciplineChain` now supports the forward chain rule (tangent sweep, `ChainDerivationMode.FORWARD`) in addition to the reverse chain rule (`ChainDerivationMode.REVERSE`). `ChainDerivationMode.AUTO` selects the mode based on the total input vs output sizes.
