Exporting a design space to an existing node of an HDF file with `DesignSpace.to_hdf(..., append=True)` now raises an explicit error when the variables, their sizes or their types differ, instead of leaving the file half-written; the bounds and the current value are still overwritten, and a design space without current value no longer keeps the value of the previous export.
