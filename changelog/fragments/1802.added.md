The `generate_coupling_graph` function and the `render_full_graph` and `render_condensed_graph` methods of `DependencyGraph` accept a `show_edge_labels` argument to hide coupling variable names from graph edges.
