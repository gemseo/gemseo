Building an `OptAsMDOScenario` from a design space in which a variable is already named `x_0`, `x_1`, ... no longer renames part of the design space before failing; the design variables are now renamed through temporary names, so that the renaming always completes.
