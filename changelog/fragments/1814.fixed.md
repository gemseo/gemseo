The Jacobian of `RBFRegressor` is now correct for the `linear`, `cubic`, `quintic`, and `thin_plate` kernels; previously, these derivatives included spurious `epsilon` scaling factors that do not appear in the corresponding SciPy kernel definitions.
