`LoggingConfiguration` has a new `configure_root_logger` option (default `False`)
to also configure the root logger,
so that logs from client code and third-party libraries are emitted with GEMSEO's settings.
