BREAKING CHANGES:

- `JobSchedulerDisciplineWrapper` and its module `discipline_wrapper` were renamed to `JobSchedulerDiscipline` and `discipline` respectively.
- `JobSchedulerDisciplineWrapperFactory` was renamed to `JobSchedulerDisciplineFactory`.
