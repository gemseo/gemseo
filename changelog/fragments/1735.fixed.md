`Lines` can use the `labels` field of `Lines_Settings`.
Plotly `BarPlot` annotations are now rounded to `n_digits` and styled with `font_size`, `textposition`, and `textangle`, aligning the Plotly backend with the Matplotlib one.
