- The boolean option `kde` of the `PairPlot` was renamed to `use_kde`.
- The histograms on the diagonal (when `use_kde` is `False`) are superimposed histograms by category when `classifier` is set.
- The density functions on the diagonal (when `use_kde` is `True`) are superimposed density functions by category when `classifier` is set.
