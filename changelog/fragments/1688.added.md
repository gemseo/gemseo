The `wrap_scenario_in_job_scheduler` function of `gemseo.scenario`, also available from `gemseo`, allows wrapping an `EvaluationScenario` (and hence an `MDOScenario`) for remote execution on an HPC cluster via a job scheduler; its `adapter_settings` argument allows customizing the underlying scenario adapter, e.g. to save the database of the scenario.
