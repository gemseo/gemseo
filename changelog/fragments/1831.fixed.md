`XLSDiscipline` now retries launching Excel with a short backoff so that the `CoCreateInstanceEx` "Server execution failed" error raised by Windows under concurrent Excel launches (parallel DOE plus threaded MDA) no longer fails the run.
